<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Remove WRITE_EXTERNAL_STORAGE if targeting API 29+ and using scoped storage properly -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/> -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResQAI"
        tools:targetApi="31">
        <activity
            android:name=".AdminShelterEditActivity"
            android:exported="false" />
        <activity
            android:name=".ReportIncidentActivity"
            android:exported="false"
            android:label="Report New Incident"
            android:parentActivityName=".HomeActivity"> <!-- Assuming HomeActivity is the parent -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".HomeActivity" />
        </activity>
        <activity
            android:name=".model.IncidentReport"
            android:exported="false" />
        <activity
            android:name=".PrepTipsActivity"
            android:exported="false" />
        <activity
            android:name=".DisasterPrepActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBIOvXDBlppOf8AC0VPKTXTJPiVTAwiX5c" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SafeRoutingActivity" />
        <activity
            android:name=".SafePathActivity"
            android:exported="false" />
        <activity android:name=".SOSActivity" />
        <activity android:name=".IncidentReportActivity" /> <!-- Existing entry -->
        <activity android:name=".ResourceTrackerActivity" />
        <activity android:name=".MedicalInfoActivity" />
        <activity android:name=".FirstAidGuideActivity" />
        <activity android:name=".FirstAidDetailActivity" />
        <activity
            android:name=".SharedIncidentsActivity"
            android:label="@string/shared_incidents_title" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.resqai.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>